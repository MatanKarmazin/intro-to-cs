#include <stdio.h>
#define SIZE 11

int fun(int *arr, int n, int x) {
    int low = 0;
    int high = n - 1;
    int mofa;
    while (low <= high) {
        int mid  = low + (high - low) / 2;
        if ()

        if () {

        }
    }

    return mofa;
}

int main() {
    int arr[] = {1, 4, 6, 6, 6, 7, 7, 8, 11, 14, 22};
    int x = 6;
    int result = fun(arr, SIZE, x);
    printf("%d\n", result);
}